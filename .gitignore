# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem
*.swp

# debug / generated output (not meant to be committed)
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
ts-check.log
tscheck.log
ts-errors.txt
/tailwind-output.css

# local env files
.env*.local
.env

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# chromium binaries (copied from node_modules during postinstall)
chromium-bin/

# Background-removal model weights — fetched by `npm run models:fetch`.
# Kept out of git deliberately: the file is ~168 MB and belongs to the deployment,
# not the repository. The API route reads it from here (or downloads it to a temp
# cache when the deployment cannot carry it — see lib/server/background-removal.ts).
models/

# package managers — pnpm is canonical (see packageManager in package.json)
package-lock.json
yarn.lock
