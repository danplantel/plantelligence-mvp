# pnpm: use a flat (hoisted) node_modules like npm/yarn.
# Fixes Next.js webpack build failures on Windows caused by pnpm's
# symlink/junction-based node_modules duplicating React (e.g.
# "Cannot read properties of null (reading 'useContext')" during prerender).
node-linker=hoisted
